import { techBookRepository } from './repositories/techBook.repo';
import { counterRepository } from './repositories/counter.repo';

export default {
  techBook: techBookRepository,
  counter: counterRepository,
};
